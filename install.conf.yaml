- shell:
    - [git submodule update --init --recursive, Installing submodules]

- link:
    ~/.tmux.conf: tmux.conf
    ~/.tmux: tmux
    ~/.vimrc: vimrc
    ~/.extrarc: extrarc
    ~/.config/nvim: nvim
    ~/.config/kitty: kitty

